
@{
    ViewData["Title"] = "View";
}

<h2>View</h2>

<div class="row">
    <div class="col-md-4">
        <form id asp-action="Index" asp-controller="Progress">
            @Html.DropDownList("Weeks", new SelectList(Model), new { @class = "form-control" })
            <div class="form-group">
                <input type="submit" value="search" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>